# Archived Dynamic-Grasp Task Configs

These task configs are standalone historical ablations. They are not part of
the active Hydra dependency chain used by the current teacher/student, Revo2, or
falling-baton launch scripts.

Keep active dependency configs in `isaacgymenvs/cfg/task/`, even if they are not
recommended direct launch targets. See `docs/dynamic_grasp_pipeline.md`.
